# https://docs.datastax.com/en/cql-oss/3.3/cql/cql\_reference/cqlCreateTable.html
Connect to the cassandra container command line\
run cql interpreter\
check which tables are present\
```
docker exec -it cassandra-1 bash
cqlsh
describe keyspaces
```
